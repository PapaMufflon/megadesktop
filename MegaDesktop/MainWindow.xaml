<Window x:Class="MegaDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:e="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:behaviours="clr-namespace:MegaDesktop.Ui"
        Title="Mega Desktop (v0.75 beta)" Height="500" Width="800" MinHeight="500" MinWidth="500"
        Icon="pack://application:,,,/m.ico" AllowDrop="True" DragEnter="Window_DragEnter_1" Drop="Window_Drop_1">
    <Window.Resources>
        <DataTemplate x:Key="NodeViewTemplate">
            <StackPanel Orientation="Horizontal">
                <Label Content=">">
                    <Label.Style>
                        <Style TargetType="Label">
                            <Setter Property="Visibility" Value="Hidden" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=Type}" Value="1">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Type}" Value="-1">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Label.Style>
                </Label>
                <Label>
                    <Binding Path="Name" />
                </Label>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <Ribbon DockPanel.Dock="Top">
            <RibbonTab Header="Home" KeyTip="H">
                <RibbonGroup Header="Home">
                    <RibbonButton Label="Upload" Command="{Binding UploadCommand}" CommandParameter="{Binding SelectedNode, Mode=OneWay}" KeyTip="U" />
                    <RibbonButton Label="Upload Folder" Command="{Binding UploadFolderCommand}" CommandParameter="{Binding SelectedNode, Mode=OneWay}" KeyTip="F" />
                    <RibbonButton Label="Download" Command="{Binding DownloadCommand}" CommandParameter="{Binding SelectedNode, Mode=OneWay}" KeyTip="D" />
                    <RibbonButton Label="Delete" Command="{Binding DeleteCommand}" CommandParameter="{Binding SelectedNode, Mode=OneWay}" KeyTip="R" />
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>
        <DockPanel DockPanel.Dock="Top" Height="100" HorizontalAlignment="Stretch" Name="dockPanel1" VerticalAlignment="Top">
            <Button Height="50" Command="{Binding UploadCommand}"
                    CommandParameter="{Binding SelectedNode, Mode=OneWay}" Width="50" HorizontalAlignment="Left"
                    ToolTip="Upload" Margin="10,0,0,0">
                <Image Source="pack://application:,,,/MegaDesktop;component/resources/upload.png" />
            </Button>
            <Button Height="50" Command="{Binding UploadFolderCommand}"
                    CommandParameter="{Binding SelectedNode, Mode=OneWay}" Width="50" HorizontalAlignment="Left"
                    ToolTip="Upload" Margin="10,0,0,0">
                <Image Source="pack://application:,,,/MegaDesktop;component/resources/folderUpload.png" />
            </Button>
            <Button Height="50" Command="{Binding DownloadCommand}"
                    CommandParameter="{Binding SelectedNode, Mode=OneWay}" Width="50" HorizontalAlignment="Left"
                    ToolTip="Download">
                <Image Source="pack://application:,,,/MegaDesktop;component/resources/download.png" />
            </Button>
            <Button Height="50" Command="{Binding DeleteCommand}"
                    CommandParameter="{Binding SelectedNode, Mode=OneWay}" Width="50" HorizontalAlignment="Left"
                    Margin="10,0,0,0" ToolTip="Delete">
                <Image Source="pack://application:,,,/MegaDesktop;component/resources/remove.png" Height="26" />
            </Button>
            <Button Height="50" Command="{Binding LoginCommand}" Width="50" Margin="10,0,0,0" ToolTip="Login">
                <Image Source="pack://application:,,,/MegaDesktop;component/resources/login.png" Height="36" />
            </Button>
            <Button Height="50" Command="{Binding LogoutCommand}" Width="50" Margin="10,0,0,0" ToolTip="Logout">
                <Image Source="pack://application:,,,/MegaDesktop;component/resources/logout.png" Height="20" />
            </Button>
            <Button Height="50" Command="{Binding RefreshCommand}" Width="50" HorizontalAlignment="Left"
                    ToolTip="Refresh" Margin="10,0,0,0">
                <Image Source="pack://application:,,,/MegaDesktop;component/resources/refresh.png" Height="40" />
            </Button>
        </DockPanel>
        <DataGrid DockPanel.Dock="Bottom"
                  ItemsSource="{Binding Transfers}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="None"
                  MaxHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ProgressBar Width="100" Maximum="100" Value="{Binding Progress, Mode=OneWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding Status}" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Cancel" Command="{Binding CancelCommand}" CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel DockPanel.Dock="Bottom" Height="25" VerticalAlignment="Bottom">
            <TextBox Padding="3" HorizontalAlignment="Left" Text="{Binding Status.Message}" FontSize="14"
                     IsReadOnly="True" BorderThickness="0" />
        </DockPanel>
        <DockPanel HorizontalAlignment="Stretch" Name="dockPanel2" VerticalAlignment="Stretch">
            <TreeView DockPanel.Dock="Left" ItemsSource="{Binding RootNode.ChildNodes}">
                <e:Interaction.Behaviors>
                    <behaviours:BindableSelectedItemBehavior
                        SelectedItem="{Binding SelectedTreeNode, Mode=OneWayToSource}" />
                </e:Interaction.Behaviors>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding ChildNodes}">
                        <TextBlock Text="{Binding Name}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            <ListBox ItemsSource="{Binding SelectedTreeNode.Children}"
                     SelectedItem="{Binding SelectedListNode, Mode=OneWayToSource}"
                     ItemTemplate="{StaticResource NodeViewTemplate}">
                <ListBox.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick"
                                  Command="{Binding SelectedListNodeActionCommand}"
                                  CommandParameter="{Binding SelectedListNode}" />
                </ListBox.InputBindings>
            </ListBox>
        </DockPanel>
    </DockPanel>
</Window>